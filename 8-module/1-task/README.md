# Учебный проект: Иконка корзины

"Корзина" — компонент интерфейса онлайн-магазина или ресторана, который используется для визуализации процесса добавления товаров для заказа.

В нашем проекте данный интерфейс состоит из двух частей.

## Компонент "Иконка корзины"
Иконка отображается на странице, когда пользователь добавляет какой-либо товар в корзину. 
Иконка исчезает, когда все товары удалены из корзины.
В соответствующей папке находится файл `cart-icon.js`, в котором хранится класс `CartIcon`, описывающий этот компонент.
С помощью данного класса реализована только "отрисовка" корзины.
Необходимо добавить позиционирование иконки при прокрутке страницы, таким образом, чтобы иконка корзины была всегда видна.

Исходный файл `cart-icon.js`:

```js
import createElement from './create-element.js';

export default class CartIcon {
  constructor() {
    this.render();
    this.addEventListeners();
  }

  // Отрисовать пустую иконку корзины
  render() {
    if (!this.elem) {
      this.elem = createElement(this.getProductCard());
    }
    return this.elem;
  }

  // Заполнить обязательные поля данными
  getCartIcon() {
    return `
    <div class="cart-icon cart-icon_visible">
        <div class="cart-icon__inner">
          <span class="cart-icon__count">1</span>
          <span class="cart-icon__price">€2.00</span>
        </div>
    </div>
    `;
  }

  addEventListeners() {
    document.addEventListener('scroll', () => this.updatePosition());
    window.addEventListener('resize', () => this.updatePosition());
  }

  // позиционировать иконку корзины на экране
  updatePosition() {
    // ваш код ...
  }
}
```

Использование иконки корзины:

```js
// создаём экземплар класса
let cartIcon = new CartIcon();

// добавляем на страницу
cartIconHolder.append(cartIcon.elem);

```

Как видно из примера в файле `index.html`, базовое отображение корзины уже работает.
Но если мы прокрутим страницу вниз, то увидим, что иконка корзины останется вверху и станет недоступной.
Необходимо сделать так, чтобы пользователи видели иконку корзины постоянно, независимо от того, на сколько они прокрутили страницу.
Это и предстоит реализовать.

## Метод updatePosition()

Реализуйте метод `updatePosition()`, который будет перемещать иконку корзины вниз и, таким образом, делать её видимой, во время прокрутки страницы.
В коде класса `CartIcon` этот метод вызывается в обработчиках событий `scroll` и `resize` (они добавляются в методе `addEventListeners`).

### Требования к перемещению:

Иконка корзины изначально находится вверху справа.
Перед тем, как переместить иконку, нужно убедиться, что иконка корзины **видна** на странице.
Это можно сделать, например, проверив метрики `offsetHeight` или `offsetWidth` корневого элемента.
Подробнее об этом можно прочитать в разделе [Размеры и прокрутка элементов: offsetWidth/Height](https://learn.javascript.ru/size-and-scroll#offsetwidth-height)

Перемещать иконку корзины нужно после того, как верхний край иконки пересёк верхнюю границу экрана при прокрутке.

**По вертикали:**
- иконка корзины должна отстоять на `50px` от верхнего края экрана.

**По горизонтали иконка должна:**
- отстоять на `20px` справа от первого элемента в документе с классом `container`.
- при этом она должна находиться не ближе `10px` от правого края окна.

Если ширина окна браузера меньше или равна `767px`, то **перемещать иконку корзины не нужно вовсе**. 
Это связанно с тем, что при такой ширине у нас используются стили для мобильной версии.
Вы сами можете в этом убедиться, открыв консоль разработчика.

Увидеть поведение иконки в действии вы можете на странице проекта <https://course-jsbasic.javascript.ru>.

***
__Важно!__

Так как это задание может быть выполнено многими способами, его достаточно сложно проверить автоматически. 
Поэтому если вы столкнетесь с ситуацией, когда в браузере всё работает как надо, но тесты не проходят, обращайтесь
к преподавателю за помощью.

## Подсказка

Далее приведено описание того, как можно решать эту задачу.
Если вы хотите решить задачу самостоятельно, не читайте дальше 😉

### Механика перемещения иконки

Основная идея перемещения иконки корзины на странице заключается в том, что если пользователь докрутил страницу до верхнего края иконки, 
то необходимо зафиксировать элемент, установив свойство (`position: fixed;`).
Подробнее про `position: fixed` [можно прочитать тут](https://learn.javascript.ru/position#position-fixed).

### Отслеживание начала перемещения

Изначально иконка позиционирована при помощи свойства `position: absolute`.
Менять позиционирование на `fixed` и перемещать иконку нужно только, когда край иконки пересёк верхнюю границу окна.
Когда страница прокручена вверх, то возвращаем иконку корзины в начальное состояние.
Для этого запомним начальные координаты верхней границы иконки. Они будут точкой отсчёта.
Когда страница прокручена вниз, позиционирование иконки определяется как `fixed`, а когда вверх — `absolute`:

```js
// текущая Y-координата относительно окна + текущая прокрутка
let initialTopCoord = this.elem.getBoundingClientRect().top + window.pageYOffset;
```

Это значение не обязательно вычислять каждый раз, достаточно вычислить его при первом вызове метода `updatePosition()`.
Полученное значение мы будем сравнивать со степенью прокрутки страницы при каждом новом вызове метода:

```js
if (window.pageYOffset > initialTopCoord) {
  // плавающая корзина
} else {
  // корзина сверху
}
```

Если значение `window.pageYOffset` будет больше `initialTopCoordinate`, значит пользователь уже докрутил до верхнего края иконки корзины, 
и нам нужно поменять её позиционирование на фиксированное, и наоборот.

### Вычисление отступа по горизонтали

Чтобы получить отступ слева, необходимо вычислить два значения:

1. Значение отступа (`20px`) справа от первого элемента с классом `container` в документе. 
Для этого возьмём расстояние до правого края этого элемента с классом `container` и прибавим к нему `20px`:

```js
document.querySelector('.container').getBoundingClientRect().right + 20;
```

2. Значение отступа от правого края экрана (`10px`). 
Для этого вычтем ширину иконки корзины(`this.elem.offsetWidth`) из общей ширины страницы (`document.documentElement.clientWidth`) и 
размер отступа от края (`10px`):

```js
document.documentElement.clientWidth - this.elem.offsetWidth - 10;
```

Теперь нужно выбрать одно значение из двух.
Правильным значением для нас будет меньшее из них.
Сделаем это с помощью метода `Math.min`:
```js
let leftIndent = Math.min(
  document.querySelector('.container').getBoundingClientRect().right + 20,
  document.documentElement.clientWidth - this.elem.offsetWidth - 10
) + 'px'
```

### Применение фиксированного позиционирования

Итак, чтобы зафиксировать иконку корзины на странице, нужно выполнить следующее:

```js
let leftIndent = Math.min(
  document.querySelector('.container').getBoundingClientRect().right + 20,
  document.documentElement.clientWidth - this.elem.offsetWidth - 10
) + 'px'

Object.assign(this.elem.style, {
  position: 'fixed',
  top: '50px',
  zIndex: 1e3,
  right: '10px',
  left: leftIndent
});
```

Не забудьте всё вернуть в начальное состояние, когда пользователь прокрутит страницу вверх:

```js
Object.assign(this.elem.style, {
  position: '',
  top: '',
  left: '',
  zIndex: ''
});
```

### Мобильные устройства

Для мобильных устройств или просто для небольшой ширины окна у нас используются другие стили, где иконка корзины всегда
позиционирована фиксированно.
Поэтому если ширина экрана меньше или равна `767px`, то позиционирование нужно вернуть к исходным значениям.
Чтобы это определить используем свойство `clientWidth` всего документа:

```js
let isMobile = document.documentElement.clientWidth <= 767;

// Если условие выполняется, очищаем свойтва
if (document.documentElement.clientWidth <= 767) {
  Object.assign(this.elem.style, {
    position: '',
    top: '',
    left: '',
    zIndex: ''
  });
}
```
